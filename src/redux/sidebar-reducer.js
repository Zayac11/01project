
let initialState = {
    sidebarPage: [
        {id: '1', name: 'Oleg'},
        {id: '2', name: 'Andrew'},
        {id: '3', name: 'Artem'}
    ],
};

const sidebarReducer = (state = initialState, action) => {

    return state;
}
export default sidebarReducer;